import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'kalinga-at-yakap-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
